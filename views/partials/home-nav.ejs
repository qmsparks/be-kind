<!-- HOME NAV -->
<nav>
    <div id="home-nav">
        <% if (user) { %>
        <p id="welcome">Welcome, <%= user.name %></p>
        <form action="/logout?_method=DELETE" method="POST">
            <input type="submit" class="cta-btn" value="Logout">
        </form>
        <% } else { %>
        <a class="nav-link" href="/login">Login</a>
        <a class="nav-link" href="/sign-up">Register</a>
        <% } %>
    </div>
</nav>